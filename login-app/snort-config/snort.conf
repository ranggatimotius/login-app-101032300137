-- ══════════════════════════════════════════════════════════════
-- snort.conf — Snort 3 (Lua) Configuration
-- NIM: 101032300137
-- ══════════════════════════════════════════════════════════════

HOME_NET = 'any'
EXTERNAL_NET = 'any'

ips =
{
    include = '/etc/snort/rules/local.rules'
}

-- Output alert ke file alert_fast.txt di log directory
alert_fast =
{
    file = true,
    packet = false
}
