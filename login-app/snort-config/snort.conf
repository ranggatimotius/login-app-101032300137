-- ══════════════════════════════════════════════════════════════
-- snort.conf — Snort 3 (Lua) Configuration
-- NIM: 101032300137
-- ══════════════════════════════════════════════════════════════

HOME_NET = 'any'
EXTERNAL_NET = 'any'

ips =
{
    include = '/etc/snort/rules/local.rules'
}

-- Output alert langsung ke stdout (Docker Logs)
-- Ini menghindari masalah file buffering dan delay!
alert_fast =
{
    file = false
}
