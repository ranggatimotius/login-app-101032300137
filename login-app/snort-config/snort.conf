HOME_NET = 'any'
EXTERNAL_NET = 'any'

ips =
{
    variables = {
        nets = {
            HOME_NET = HOME_NET,
            EXTERNAL_NET = EXTERNAL_NET
        }
    },
    include = '/etc/snort/rules/local.rules'
}

alert_fast =
{
    file = true,
    packet = false
}
